#include "Arduino.h"
#include "Module.h"

Module::Module(int moduleType) {
  // initialise module based on its type (VCO, VCF, LFO, etc)
  switch(moduleType) {
    case 0:
    break;

    case 1:
    break;
  }
}

